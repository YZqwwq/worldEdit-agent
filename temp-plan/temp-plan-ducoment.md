# Agent 页面感知与文档编辑能力实施计划

## 目标

1. 让主 Agent 在每条用户消息开始处理时，准确知道用户当时停留的页面。
2. 文档页面需要精确到世界观、实体、当前文档及文档版本。
3. 让主 Agent 能稳定读取、创建、修改、改名、移动和删除世界观文档。
4. 防止前端自动保存与 Agent 写入互相覆盖，并让 Agent 的修改及时反映到编辑器。

## 当前进度

- [x] 建立页面上下文协议，并由 Renderer 采集当前页面状态。
- [x] 将页面快照贯通到用户消息、事件队列、Agent State 与 `contextNode`。
- [x] 为文档增加 `revision` 版本标识与乐观锁保护。
- [x] 建立 `world_document_editor` 工具集及文档读取、创建和编辑能力。
- [x] 建立文档变更广播与编辑器冲突保护。
- [x] 实机确认 Agent 能识别当前文档，并能读取持久化的完整正文。
- [ ] 优化读取文档后的表达，使工具执行过程保持静默、最终回复自然承接正文。
- [ ] 完成高风险文档操作的应用内确认协议。
- [ ] 增加 diff、版本历史与撤销能力。
- [ ] 增加 TipTap 节点级、选区级局部编辑能力。

## 核心边界

- `workspaceContext` 表示用户正在查看什么，由 Renderer 确定。
- `worldFocusContext` 表示用户正在讨论什么，由瞬时感知判断。
- 页面上下文是本轮运行数据，不写入长期记忆，也不作为记忆 slot 的事实来源。
- 每条用户消息在入队时保存页面快照。后续切换页面不能改变已入队消息的上下文。
- Renderer 只提供页面和对象 ID；主进程必须从数据库重新校验对象归属及名称。
- 文档工具只调用主进程文档服务，不直接操作 TipTap、DOM 或 Renderer 状态。

## 第一阶段：页面上下文协议

1. 在共享类型中增加 `AgentWorkspaceContext`。
2. 页面类型至少包括：首页、世界观、实体、文档、AI 对话和其他页面。
3. 上下文按页面携带 `worldId`、`entityId`、`entityType`、`documentId`、`revision` 等可选字段。
4. Renderer 建立统一的 `workspaceContextStore`。
5. Router 负责写入粗粒度页面类型，具体 View 负责补充精确对象。
6. 文档编辑器切换当前文档时，立即更新 `documentId` 和文档版本。
7. View 卸载时只清理自己持有的上下文，避免旧页面异步回调覆盖新页面。

### 检查方向

- 首页、世界观页、实体页、文档页和其他页面是否生成正确的页面类型。
- 在同一文档页面切换文件时，当前 `documentId` 是否同步变化。
- 快速切换路由时，是否出现旧页面状态覆盖新页面。
- 世界级文档和实体级文档的归属信息是否都能正确表达。
- 页面上下文中是否意外携带整篇文档正文或其他不必要的大字段。

## 第二阶段：贯通消息、事件队列与 Agent State

1. 扩展 `MainAgentUserMessageInput`，允许携带 `workspaceContext`。
2. 发送消息时获取不可变的页面上下文快照。
3. 在 `AIService.sendStreamMessage` 中校验并规范化上下文。
4. 将上下文写入 `MainAgentUserMessageEvent.payload` 并随事件持久化。
5. 恢复未完成事件时，必须恢复原始页面快照。
6. 扩展 `MessagesState`，增加只读的本轮 `workspaceContext`。
7. `mainAgentChatRuntimeService` 启动图时把事件中的上下文写入初始 State。
8. `contextNode` 将页面上下文拼入首次 LLM 上下文。
9. `toolContextReloadNode` 保留同一个页面快照，不把工具结果误当成新的页面状态。

### 检查方向

- 消息排队后切换页面，Agent 是否仍使用消息发送时的页面。
- 应用重启并恢复队列后，页面快照是否仍然存在。
- 同轮多次工具调用后，页面上下文是否保持不变。
- 用户停留在人物文档页但讨论另一个国家时，`workspaceContext` 与 `worldFocusContext` 是否能同时存在。
- `contextNode` 注入内容是否简洁，是否明显增加提示 token。
- 非法或已删除的 world、entity、document ID 是否会被主进程拒绝或降级。

## 第三阶段：文档版本与并发保护

1. 为文档表增加递增的 `revision` 字段。
2. 文档读取结果和页面上下文均返回当前 `revision`。
3. 修改、改名和移动操作必须携带 `expectedRevision`。
4. 数据库更新时使用乐观锁；版本不一致则拒绝覆盖。
5. 成功更新后原子地执行 `revision + 1`。
6. 冲突结果返回当前版本和必要元数据，要求 Agent 重新读取后再决定。

### 检查方向

- 前端自动保存和 Agent 同时修改时，是否只能有一个写入成功。
- 冲突后是否保留用户尚未保存的文本。
- 标题更新、正文更新和移动操作是否都会推进版本。
- 单纯读取是否不会改变版本。
- 旧客户端或缺少 `expectedRevision` 的写入是否被明确拒绝。

## 第四阶段：文档工具集

新增 `world_document_editor` 能力包，先实现稳定的整篇读取和写入：

1. `list_world_documents`：读取指定世界观或实体的文档目录。
2. `read_world_document`：读取单个文档及其版本。
3. `create_world_document`：在指定归属和父级下创建文档。
4. `update_world_document`：更新标题或 HTML 正文，要求版本匹配。
5. `rename_world_document`：明确的轻量改名入口。
6. `move_world_document`：调整父级和排序。
7. `delete_world_document`：删除文档或子树，必须经过高风险确认。

工具注册要求：

- 读取工具标记为 `read`，写入工具标记为 `write`。
- 工具集可被工具底图发现；文档页面可按任务上下文激活少量常用工具。
- 不把全部文档工具永久挂载到主 Agent。
- 每个工具提供阶段提示，例如“正在读取当前文档”“正在更新文档内容”。
- 工具结果返回紧凑结构：状态、文档 ID、修改字段、前后版本和摘要。
- 第一阶段不实现复杂的富文本局部 patch，先保证整篇 HTML 稳定读写。

### 检查方向

- Agent 是否能根据页面上下文正确使用“当前文档”，而不要求用户重复提供 ID。
- 非文档页面能否通过世界观、实体和标题稳定解析目标文档。
- 工具调用结果是否进入 `toolEvidenceContext`，并在下一轮被正确感知。
- 成功写入后 Agent 是否停止重复调用同一工具。
- 工具是否会越过 world、owner 或 parent 的归属约束。
- 删除、递归删除和移动到后代节点是否被正确拦截。
- 工具返回是否避免携带不必要的整篇正文，防止上下文膨胀。

## 第五阶段：编辑器同步

1. 主进程文档写入成功后广播 `worldEntityDocument:changed`。
2. 事件至少包含文档 ID、变更类型和新版本。
3. 当前编辑器无本地未保存修改时，自动重新读取。
4. 当前编辑器存在本地修改时，不自动覆盖，显示外部更新冲突。
5. 修改其他文档时，只刷新目录对应项。
6. 删除当前文档时，切换到合理的相邻文档或空状态。

### 检查方向

- Agent 更新当前文档后，编辑器是否及时显示新内容。
- 编辑器聚焦和输入状态是否因刷新而丢失。
- 本地脏数据是否会被 Agent 写入事件覆盖。
- 标题变化是否同步到目录、顶部栏和大纲。
- 删除当前文档后是否仍能选择并编辑其他文档。
- 多窗口同时打开同一文档时是否遵循相同的版本冲突规则。

## 第六阶段：联调与验收

1. 首页询问当前页面，Agent 能识别为首页。
2. 世界观页询问当前对象，Agent 能识别对应世界观。
3. 实体页询问当前对象，Agent 能识别实体及类型。
4. 文档页切换文件后发送消息，Agent 能准确识别当前文档。
5. 消息入队后切换页面，Agent 仍使用原始页面快照。
6. Agent 能读取当前文档并基于正文回答。
7. Agent 能创建和修改文档，前端自动刷新。
8. Agent 与前端并发修改时触发冲突，不发生静默覆盖。
9. 删除操作必须出现应用内确认，取消后不产生数据库变化。
10. 完整工具循环不出现重复写入、无限调用或 LangGraph 递归上限错误。

## 第七阶段：阅读与页面感知的表达自然化

### 当前验证结论

- 页面感知链路有效，Agent 能识别当前世界观与文档编辑页面。
- 文档读取链路有效，Agent 能读取当前文档正文并基于内容继续讨论。
- 当前问题属于表达策略，而不是页面上下文丢失、工具不可见或正文读取失败。

### 实现方向

1. 将 `workspaceContext` 和工具返回视为内部证据，不作为最终回复的固定表述模板。
2. 除非用户明确询问调试信息或版本状态，否则不主动输出文档 ID、`revision`、工具名等内部字段。
3. 避免反复表述“我已经读取”“工具返回”“当前版本是”等过程信息，直接基于正文回答问题。
4. 用户询问当前位置时，简洁回答“世界观 / 分类或实体 / 文档”，不附带无关的系统细节。
5. 用户询问文档内容或评价时，静默完成读取，在最终回复中自然总结、分析或表达看法。
6. 区分事实概括与主观评价。主观评价可以受人格影响，但应自然地以文档内容为依据。
7. 不在每次回复末尾机械追加“是否继续测试”或“要不要进一步处理”；只有确实需要用户决策时才追问。
8. 工具调用阶段提示继续显示在对话 UI 中，用于让用户感知进度，但最终消息不再重复播报调用过程。
9. 工具使用约束放入 `contextNode` 的工具说明；语气与措辞交给表达层处理，避免人格提示承担工具策略。
10. 同一轮已有有效读取结果时直接使用；跨轮仅在缺少正文或 `revision` 已变化时重新读取，避免重复调用。

### 检查方向

1. 询问“我在哪”时，只返回准确的位置名称，不输出 ID、工具名或版本号。
2. 询问“这篇写了什么”时，读取后直接概括正文，不先说明读取动作。
3. 询问“你觉得这个设定怎么样”时，直接给出有依据的评价，不先完整复述工具过程。
4. 只有用户明确询问文档版本时才输出 `revision`。
5. 调试模式下仍可查看页面快照、工具调用、文档 ID 与版本信息。
6. 连续追问同一文档时不发生无意义的重复读取，也不因工具仍可见而进入循环。
7. 页面或文档切换后，下一条消息使用新的页面快照，不沿用上一文档的内容判断。

## 后续优化

- 基于 TipTap JSON 增加标题、段落和节点级局部编辑。
- 支持当前选区、光标位置和选中文本上下文。
- 支持批量文档重构及可撤销变更集。
- 为 Agent 写入生成 diff、变更说明和版本历史。
- 根据页面类型动态激活工具集，进一步降低常驻工具 token。
